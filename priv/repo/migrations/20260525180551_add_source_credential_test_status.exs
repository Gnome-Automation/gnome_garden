defmodule GnomeGarden.Repo.Migrations.AddSourceCredentialTestStatus do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:procurement_source_credentials) do
      add :test_status, :text, null: false, default: "untested"
      add :last_test_queued_at, :utc_datetime
      add :last_test_started_at, :utc_datetime
      add :last_test_completed_at, :utc_datetime
      add :last_test_procurement_source_id, :uuid
    end
  end

  def down do
    alter table(:procurement_source_credentials) do
      remove :last_test_procurement_source_id
      remove :last_test_completed_at
      remove :last_test_started_at
      remove :last_test_queued_at
      remove :test_status
    end
  end
end
